# Json messages

# Each line that starts with a '#' is ignored
# Each message line consist in an identifier and a value, separated by a '='
# The value is the json format of a minecraft message

# For example:
# hello={"text":"Hello !"}
# store the message "Hello !" with the id 'hello'
